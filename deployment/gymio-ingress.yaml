apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: gymio
  annotations:
    ingress.gcp.kubernetes.io/pre-shared-cert: "gymio2"
spec:
  backend:
    serviceName: gymio
    servicePort: 443